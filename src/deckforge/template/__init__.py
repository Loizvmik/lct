"""Разбор шаблона .pptx в дизайн-систему: тема, детект заглушек, фактическая палитра.

Детерминированный слой, как и ooxml/, — с двумя исключениями (было одно, до
Task 18 — см. ниже, почему граница расширена явно, а не молча):

- `naming.py` вызывает модель для именования ролей палитры (`brand`,
  `surface`, `on_surface`, ...);
- `vision_kind.py` (Task 18) вызывает мультимодальную модель, чтобы уточнить
  вид раскладки (`Pattern.kind`) по отрисованной картинке слайда-примера —
  геометрический майнинг (`patterns.py::_classify_kind`) различает только
  семь корзин формы (повтор/колонки/площадь картинки), не смысл композиции
  (цитата, фото с текстом, крупный фактоид с подписью геометрически
  неотличимы от обычного текстового слайда и схлопываются в `section`/
  `bullets` — ручной разбор задачи, VK Education: 22 из 33 намайненных
  раскладок). Основание расширения — критерий оценки конкурса прямо
  называет «CV/LLM-оркестрацию» частью решения, не только детерминированный
  разбор OOXML.

Это архитектурная граница проекта — `naming.py` и `vision_kind.py` ЕДИНСТВЕННЫЕ
модули пакета, которые реально ЗОВУТ модель (закреплено `tests/template/
test_architecture.py`, AST-проверкой импортов `deckforge.provider` по всем
модулям пакета). `patterns.py` сам остаётся детерминированным, как и раньше
(докстрока модуля это заявляет буквально) — уточнение вида происходит НЕ
внутри него, а отдельным проходом `profile.py::TemplateProfile.from_file`
ПОВЕРХ уже намайненных им паттернов, тем же принципом "модель предлагает,
код проверяет", что и у `naming.py`. `profile.py` сам импортирует из
`deckforge.provider` только типы `LLMProvider`/`VisionProvider` для сигнатур
параметров `namer`/`vision` у `TemplateProfile.from_file` — самих вызовов
там нет, они живут в `naming.py`/`vision_kind.py`.
"""
