"""Разбор шаблона .pptx в дизайн-систему: тема, детект заглушек, фактическая палитра.

Детерминированный слой, как и ooxml/, — с одним исключением: `naming.py`
вызывает модель для именования ролей палитры (`brand`, `surface`,
`on_surface`, ...). Это архитектурная граница проекта — `naming.py`
единственный модуль пакета, который реально ЗОВЁТ модель. `profile.py`
тоже импортирует из `deckforge.provider`, но только тип `LLMProvider` для
сигнатуры параметра `namer` у `TemplateProfile.from_file` — самого вызова
там нет, он живёт только в `naming.py`.
"""
